services:
  - type: web
    name: postcode-heatmap
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run postcode_heatmap_app.py --server.port=$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
